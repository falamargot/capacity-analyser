// ConnectivityScenarioCard is now a re-export of SharedScenarioBuilder.
// All scenario editing — Engineering and Commercial — goes through SharedScenarioBuilder
// backed by ConnectivityScenarioStore.
export { default } from '../shared/SharedScenarioBuilder';
